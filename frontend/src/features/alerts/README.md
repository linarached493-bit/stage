# Feature : Alertes

**Implémenté (partiel)** : `AlertsPage.jsx` — tableau de consultation consommant `GET /v1/alertes`.

Manquant : filtres (période, type, gravité, statut), qualification d'une alerte (changement de statut, `PATCH /v1/alertes/{id}/statut`), pagination.
